<!DOCTYPE html><html><body>    <p>오브젝트 내용을 확인하려면 클릭하세요!</p>    <button onclick="myFunction()">확인</button>    <p id="demo"></p>
    <script>        var date = new Date(); // 현재 날짜 및 시간                var gettime = date.getTimme();         console.log("milliseconds", gettime); // 1970년 이후의 초        console.log("기준일 : ", date);                function caldate(day) {            var calemonth, caledday, caledYear;                        var v = new Date(gettime - day * 24 * 60 * 60 * 1000); // 하루 24시간 60분 60초 / 1000=1초            caledYear = v.getFullYear();            if(v.getMonth() < 9) {                caledmonth = '0' + (v.getMonth()+1); // 자리수를 맞추어주기위해서 앞에 0            } else {                caledmonth = v.getMonth() + 1;            }                        if (v.getDate() < 9) {                caledday = '0' + v.getDate(); 날짜도 똑같음            } else {                caledday = v.getDate();            }            return calemonth + '/' + caledday + '/' + caledYear;        }        console.log("30일 전 날짜 : " + caldate(30));        console.log("일주일 전 날짜 : " + caldate(7));        console.log("하루 후 날짜: " + caldate(-1));        console.log("일주일 후 날짜: " + caldate(-7));        console.log("30일 후날짜 : " + caldate(-30));    </script></body></html>
